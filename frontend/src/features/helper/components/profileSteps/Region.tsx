import React from 'react'
import type { RegionFormSchema } from '@helper/types'
import { useFormValidation } from '@hooks'

type Props = {}

const Region = (props: Props) => {
  return (
    <div>Region</div>
  )
}

export default Region